# Patches/Core/ — Core例外パッチ台帳

このディレクトリは、`CLAUDE.md`「Core File Modification Policy」の★狭い例外
(2026-07-30追加、D-6/P465振り返り + Fable5クロス監査。2026-08-16、Class B追加、
P602/D-57振り返り + Fable5クロス監査 + ユーザー明示承認)に基づき提案された、
`Core/px68k/`・`Core/c68k/`向けパッチファイルのみを置く。

**重要: このディレクトリのパッチはAIエージェントが自動適用することはない。**
`Core/`へのEdit/Writeは`PreToolUse`フックで常時ブロックされており、フックに
例外トークンや一時無効化の仕組みは存在しない(意図的——CLAUDE.mdの
「Workaround-Twiceルール」参照)。**適用は必ずユーザー自身が
手動で`git apply Patches/Core/pXXX_....patch`を実行する。**

## パッチの2分類

- **Class A**: 参照実装からの実証済み修正のverbatim移植。
- **Class B**(2026-08-16追加): 参照実装のいずれにも修正が存在しない
  (共有の上流欠陥等)場合、実機仕様書を一次情報源とし、実行時ログで
  機構そのもの(症状だけでなく)を確認した上での修正。Class Aより
  高い証拠バーを課す代わりに、参照実装への依存を外す。頻度トリップワイヤは
  両クラス合算でカウントする。

## パッチファイルのヘッダ形式(必須、クラスにより異なる)

### Class A

```
# 分類: A
# 出典リポジトリ: (例: MPX68K https://github.com/YosAwed/MPX68K)
# 参照コミットハッシュ: (該当箇所を引用した時点のコミットハッシュをピン止め)
# 対象ファイル:行範囲: (例: Core/px68k/x68k/adpcm.c:XXX-YYY)
# 理由(1行): (例: ADPCM停止時のOldL/OldRリセット漏れによる無音時クリック対策)
# 由来サイクル: PXXX
```

### Class B

```
# 分類: B(ハードウェア仕様準拠修正——参照実装に既存修正なし)
# 一次資料: <書名+ページ番号+該当文の引用>(実際にそのページを開いて確認したこと
#   ——記憶からの引用は不可、referent-binding準拠)
# 実測根拠: [runtime log] ログID・再現条件・観測値と予測値の一致(症状ではなく
#   機構そのものの数値一致であること)
# 上流欠陥同定: (共有欠陥の場合、その出所——コード生成器・共通祖先等)
# 同系先例: (同一ファイル/同一プロジェクト内に存在する、同種の正しい実装があれば)
# 参照実装調査(修正不在の証明): 参照ごとに——リポジトリ名、読んだコピーの
#   pinned commit hash、同一欠陥の所在file:line
# Bridge代替検討: 却下理由(1行)
# 対象ファイル:行範囲:
# 由来サイクル: PXXX
```

## 適用条件(要約、詳細はCLAUDE.md本体)

1. Class Aは**実証済み修正のverbatim移植のみ**。Class Bは上記4条件
   (機構の実行時確認・一次資料引用・修正不在調査・Bridge代替検討)を
   全て満たすこと——いずれの分類も新規診断のみに基づく独自修正
   (Class Bの4条件を満たさないもの)は対象外。
2. 対象範囲の全diffを添付(2行だけの切り出しは不可)。
3. サイクル途中の「レスキュー」としての提案は不可——新規Fix Planの
   冒頭からのみ。
4. パッチの効果そのものをhands-on確認することがマージ(適用)前提条件。
   効かなければ適用しない。
5. 効果確認後、置き換え対象だったBridge側近似コードは同一/直後サイクルで
   撤去する(重ね着禁止)。
6. `c68k/`・CPU実行/タイミングに関わるCore編集は、通常のCode Reviewに
   加えてCodex/Fable5のクロスファミリーレビューを必須とする。
7. **Class Bという分類自体の採否は、個別パッチの承認とは別に、
   ユーザーへ明示的・単独の質問として提示すること**——「このパッチを
   承認しますか」に紛れ込ませて分類の新設を黙認させてはならない
   (自己承認リスク、2026-08-16のP602サイクルでハーネス自体が検出)。

## 台帳(適用状況)

| パッチ | 分類 | 由来 | 状態 |
|---|---|---|---|
| `p603_c68k_illegal_pc_correction.patch` | A | P603 | 適用済み |
| `p622_crtc_r20_bit3_mask_removal.patch` | A | P622 | 適用済み |
| `p639_zero_division_guard.patch` | A | P639 | **★取り下げ(SUPERSEDED BY P641)** — 内容はp641へ全て吸収。単独適用は不要かつ不可(同一箇所で衝突) |
| `p641_crtc_dynamic_timing.patch` | A | P641 | **適用済み**(2026-08-20、main=e25d7a8)。★状態欄の是正: P671まで「未適用」と記載されていたが実際は適用済みだった。根拠 = `Core:` `Core/px68k/x68k/crtc.c:306,381`にP641コメントが在中 / `git log -- Core/px68k/x68k/crtc.c`の`4ea686d P641: D-65本体実装`。なお本パッチを適用しないとビルドが失敗する(pbxprojが`crtc_timing.c`を参照するため) |
| `p657_crtc_raster_copy_timing.patch` | A | P657 | **適用済み**(2026-08-22、main=522a73f)。★状態欄の是正: P671まで「未適用」と記載されていたが実際は適用済みだった。根拠 = `Core:` `Core/px68k/x68k/crtc.c:127,135,163,296,544,562`にP657コメントが在中 / コミット`5aae5a2`。なお本パッチを適用しないとビルドが失敗する(Bridgeが`CRTC_HorizontalFrontPorch()`を呼ぶため) |
| `p671_crtc_rastercopy_compat_trigger.patch` | A | P671 | **適用済み**(2026-08-23、main=fd34997)。★状態欄の是正(P686): 「未適用(ユーザーが`git apply`)。★T-4のユーザーhands-on確認が通るまで『解決』扱いにしない」と記載されていたが、実際は適用済みだった。根拠 = `Core:` `Core/px68k/x68k/crtc.c:303,550,578,580`にP671のコードとコメントが在中(`grep -n "P671" Core/px68k/x68k/crtc.c`) / `git log --oneline -3 -- Core/px68k/x68k/crtc.c`の`fd34997 P671: D-69解決(CRTCラスタコピー互換トリガ復活、MPX68K f59c07dのClass A移植)`。D-69は`Docs/09`で解決済み・mainマージ済み。内容: D-69(P657の実行契機変更による回帰=ラスタコピーを使う高速テキスト出力の表示崩れ)の是正。MPX68K `f59c07d`のverbatim移植で、`p657`のフロントポーチ実行を維持したまま即時トリガを互換措置として復活させる(`p657`適用済みが前提) |
| `p686_fdc_drive_count.patch` | **B** | P686 | **適用済み**(2026-08-25、main=`f7a5933`)。D-70(FD2/FD3にディスクイメージをマウントしてもHuman68kがドライブを認識しない)の是正。実機仕様(テクニカルデータブック 印刷 p.157 5-5(3)「RECALIBRATEをFDD1〜4、4台とも」)に準拠させ、`fdc.c`のRecalibrate判定(`fdc.c:271`が drv 2/3 へ無条件でST0 bit4(EC=未接続)を返していた)と$E94005Hのドライブ選択マスクを4台対応化。**★Class B分類の初適用**(既存6本はすべてClass A) — 参照実装4種(px68k本家 / px68k-libretro / MPX68K / XM6)がすべて同一の欠陥を共有し、いずれも修正を持たないため、移植元となるClass Aの前例が存在しなかった。既定は外付け未装着(2台)のままで、`FDC_SetDriveCount(2)`のとき挙動は適用前とビット単位で同一(T-1/T-3のsmoke gateプローブ値一致で確認)。ユーザーhands-on確認済み(2026-08-25、トグルON+ハードリセットでC:/D:の読み書き成功=T-4/T-7、トグルOFFで非表示=T-5) |

## ★台帳の状態欄の記入規律(恒久ルール、P686で成文化・ユーザー承認済み)

**台帳の状態欄は、そのパッチを含むサイクルの Step 6 コミット時点の事実を書く。
パッチ作成時点の「未適用」のまま放置してはならない。Build & Test Agent は
`git diff -- Core/` の検証(ポリシー条件7)を行う際、同時に台帳の状態欄が
実態と一致しているかを確認する。**

背景: 同一の乖離が **3 回**発生している(`p641`・`p657` の 2 件を P671 が是正 →
その P671 自身の行が P686 まで「未適用」のまま放置されていた)。構造的原因は
**「パッチファイルを書いた時点」と「ユーザーが `git apply` した時点」の間に
台帳を更新する担当者が居ない**こと — パッチ作成者はまだ適用されていないので
「未適用」と書くのが正しく、適用したユーザーは台帳を触らない、という担当の空白である。
Step 6 のコミットは hands-on 確認の完了後に 1 回だけ打たれるため、その時点で
「適用済み(日付、main=ハッシュ)」を書けば「未適用 → 適用済み」の 2 段階更新は
そもそも発生しない。

## 頻度トリップワイヤ

1フェーズあたりパッチ3件超(Class A/B合算)、またはBridge側検討より先に
第一候補として提案された場合は、本ポリシー自体の再審査を行う。
